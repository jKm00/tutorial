# Optimistic Updates w/ TanStack Query

An example application showcasing how to show optomistic updates to the UI with the use of TanStack queries and mutations.

## Getting Started

1. Clone repo
2. Install dependencies

```
pnpm install
```

3. Run dev server

```
pnpm dev
```
